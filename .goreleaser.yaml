version: 2

project_name: chief-mcp

before:
  hooks:
    - go mod tidy

builds:
  - id: chief-mcp
    main: .
    binary: chief-mcp
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{ .Version }}
    goos:
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: chief-mcp
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats:
          - zip

homebrew_casks:
  - name: chief-mcp
    binaries:
      - chief-mcp
    repository:
      owner: Storytell-ai
      name: homebrew-tap
      branch: main
    homepage: "https://github.com/Storytell-ai/chief-mcp"
    description: "Chief MCP server"
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/chief-mcp"]
          end

changelog:
  sort: asc
  use: github
  format: "{{ .Message }}"
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
  groups:
    - title: New Features
      regexp: "^.*feat[:(].*$"
      order: 0
    - title: Bug Fixes
      regexp: "^.*fix[:(].*$"
      order: 1
    - title: Others
      order: 999
